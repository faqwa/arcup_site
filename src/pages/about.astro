---
import BaseLayout from "../layouts/BaseLayout.astro";
import SiteHeader from "../components/SiteHeader.astro";
import PageNavigation from "../components/PageNavigation.astro";
import Card from "../components/Card.astro";
import LinkButton from "../components/LinkButton.astro";
import { withBase } from '../utils/paths';
import { buildSEO } from '../utils/seo';

const seo = buildSEO({
  title: "About ArcUp",
  description: "An open experimental research framework exploring how plasma technologies can support ecological repair. Part of the broader Repractice ecosystem.",
  path: "/about",
  siteUrl: Astro.site,
});
---

<BaseLayout {...seo}>
  <SiteHeader active="about" />
  
  <main id="main-content" class="page-shell page-about-overview">
    
    <!-- Hero Section -->
    <header class="about-hero">
      <h1>About ArcUp</h1>
      <p class="about-lede" aria-label="ArcUp overview introduction">
        ArcUp is an open experimental research framework exploring how plasma technologies can support ecological repair. 
        It is part of the broader <a href="https://www.xbyali.page" target="_blank" rel="noopener">xbyali.page</a> ecosystem — 
        connecting philosophy, ethics, and experimentation through the <em>Repractice Framework</em>.
      </p>
      <p class="about-lede">
        ArcUp serves as the backbone of open plasma research, building transparent pathways between individuals, 
        institutions, and communities who want to develop technology for <em>the planet, not the market</em>.
      </p>
      <p class="about-lede-emphasis">
        ArcUp exists to make research a collective act of care.
      </p>
    </header>

    <!-- Navigation Cards Section -->
    <section class="about-section">
      <h2 class="section-title">What You'll Find Here</h2>
      <p class="section-intro" aria-label="Navigation summary of ArcUp About section">
        This section introduces the story, people, and purpose behind ArcUp. 
        Each page expands the framework's story from reflection to practice.
      </p>

      <div class="about-nav-grid">
        
        <Card as="article" class="about-nav-card">
          <div class="nav-card-content">
            <h3>Origin</h3>
            <p>How a small experiment with plasma and seeds became an open framework for regenerative research.</p>
          </div>
          <LinkButton 
            href={withBase("/about/origin")} 
            variant="outline"
            icon="arrow-right"
          >
            Read the Story
          </LinkButton>
        </Card>

        <Card as="article" class="about-nav-card">
          <div class="nav-card-content">
            <h3>Founder</h3>
            <p>Meet Ali Saleh, and the systems thinking that guides ArcUp's interdisciplinary approach.</p>
          </div>
          <LinkButton 
            href={withBase("/about/founder")} 
            variant="outline"
            icon="arrow-right"
          >
            Meet Ali
          </LinkButton>
        </Card>

        <Card as="article" class="about-nav-card">
          <div class="nav-card-content">
            <h3>Mission</h3>
            <p>What drives the work, how the phases unfold, and the vision for a commons of plasma research.</p>
          </div>
          <LinkButton 
            href={withBase("/about/mission")} 
            variant="outline"
            icon="arrow-right"
          >
            Explore the Mission
          </LinkButton>
        </Card>

      </div>
    </section>

    <!-- Living Framework Section -->
    <Card as="section" class="framework-section">
      <h2>A Living Framework</h2>
      <p aria-label="ArcUp framework description">
        ArcUp grows through open, iterative phases — from individual experiments to global collaboration. 
        It isn't a company or lab; it's a <em>commons of practice</em>, shaped by every person who contributes to it.
      </p>
      <p>
        Each experiment adds to a shared understanding of how plasma interacts with life — and how open science 
        can model a more regenerative relationship with Earth.
      </p>
      <p class="framework-closing">
        ArcUp is science re-practised: transparent, ethical, and alive.
      </p>
    </Card>

    <!-- Repractice Connection -->
    <section class="repractice-connection">
      <div class="repractice-divider" aria-hidden="true"></div>
      <div class="repractice-content">
        <h2>Connection to Repractice</h2>
        <p aria-label="Relationship between ArcUp and Repractice">
          ArcUp operates under the ethical foundation of <em>Repractice</em>: research as care, transparency as principle, 
          and collaboration as responsibility.
        </p>
        <p>
          Repractice defines the values behind ArcUp's structure — ensuring that technology development remains 
          non-extractive, interdisciplinary, and accessible to all.
        </p>
        <LinkButton 
          href={withBase("/how")} 
          variant="outline"
          icon="arrow-right"
        >
          Explore Repractice Methods
        </LinkButton>
      </div>
    </section>

    <!-- Closing Invitation -->
    <section class="closing-invitation">
      <p aria-label="Closing invitation for visitors to engage">
        ArcUp is still forming — a workbench, a library, a community in motion. 
        You're invited to explore, question, and shape it with us.
      </p>
      <p class="invitation-emphasis">
        Begin wherever you feel drawn — to the science, the story, or the ethics that hold it all together.
      </p>
    </section>

  </main>

  <PageNavigation currentPage="about" />
</BaseLayout>

<style>
  .page-about-overview {
    display: grid;
    gap: var(--section-gap);
  }

  /* Hero Section */
  .about-hero {
    max-width: 85ch;
    margin: 0 auto;
    text-align: center;
  }

  .about-hero h1 {
    margin: 0 0 1.5rem 0;
    font-size: var(--text-4xl);
    font-weight: var(--font-weight-bold);
    line-height: 1.1;
    color: var(--color-text);
  }

  .about-lede {
    margin: 0 0 1.5rem 0;
    font-size: var(--text-lg);
    line-height: 1.7;
    color: var(--color-text-muted);
  }

  .about-lede a {
    color: var(--color-primary-bright);
    text-decoration: none;
    border-bottom: 1px solid rgba(93, 225, 228, 0.3);
    transition: all var(--transition-base);
  }

  .about-lede a:hover,
  .about-lede a:focus-visible {
    color: var(--color-text);
    border-bottom-color: var(--color-primary-bright);
  }

  .about-lede-emphasis {
    margin: 2rem 0 0 0;
    font-size: var(--text-xl);
    font-weight: var(--font-weight-medium);
    color: var(--color-text);
  }

  /* About Section */
  .about-section {
    max-width: 95ch;
    margin: 0 auto;
  }

  .section-title {
    margin: 0 0 1rem 0;
    font-size: var(--text-2xl);
    color: var(--color-text);
    text-align: center;
  }

  .section-intro {
    margin: 0 0 3rem 0;
    font-size: var(--text-base);
    line-height: 1.7;
    color: var(--color-text-muted);
    text-align: center;
    max-width: 75ch;
    margin-left: auto;
    margin-right: auto;
  }

  /* Navigation Cards Grid */
  .about-nav-grid {
    display: grid;
    gap: 1.5rem;
    grid-template-columns: repeat(auto-fit, minmax(min(280px, 100%), 1fr));
  }

  .about-nav-card {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    transition: all var(--transition-base);
  }

  .about-nav-card:hover {
    transform: translateY(-4px);
  }

  .nav-card-content {
    flex: 1;
    display: grid;
    gap: 0.75rem;
  }

  .nav-card-content h3 {
    margin: 0;
    font-size: var(--text-xl);
    color: var(--color-text);
  }

  .nav-card-content p {
    margin: 0;
    font-size: var(--text-base);
    line-height: 1.7;
    color: var(--color-text-muted);
  }

  /* Framework Section */
  .framework-section {
    max-width: 85ch;
    margin: 0 auto;
  }

  .framework-section h2 {
    margin: 0 0 1.5rem 0;
    font-size: var(--text-2xl);
    color: var(--color-text);
  }

  .framework-section p {
    margin: 0 0 1.5rem 0;
    line-height: 1.7;
    color: var(--color-text-muted);
  }

  .framework-section p:last-child {
    margin-bottom: 0;
  }

  .framework-closing {
    margin-top: 2rem !important;
    padding-top: 1.5rem;
    border-top: 1px solid var(--color-border);
    font-size: var(--text-lg);
    font-weight: var(--font-weight-medium);
    color: var(--color-text);
  }

  /* Repractice Connection */
  .repractice-connection {
    max-width: 85ch;
    margin: 0 auto;
  }

  .repractice-divider {
    width: 60%;
    height: 1px;
    margin: 0 auto 3rem auto;
    background: linear-gradient(
      to right,
      transparent,
      var(--color-primary-bright),
      transparent
    );
    opacity: 0.3;
  }

  .repractice-content {
    display: grid;
    gap: 1.5rem;
  }

  .repractice-content h2 {
    margin: 0;
    font-size: var(--text-2xl);
    color: var(--color-text);
    text-align: center;
  }

  .repractice-content p {
    margin: 0;
    line-height: 1.7;
    color: var(--color-text-muted);
    text-align: center;
  }

  /* Closing Invitation */
  .closing-invitation {
    max-width: 75ch;
    margin: 0 auto;
    text-align: center;
  }

  .closing-invitation p {
    margin: 0 0 1.5rem 0;
    font-size: var(--text-lg);
    line-height: 1.7;
    color: var(--color-text-muted);
  }

  .closing-invitation p:last-child {
    margin-bottom: 0;
  }

  .invitation-emphasis {
    font-size: var(--text-xl) !important;
    font-weight: var(--font-weight-medium);
    color: var(--color-text) !important;
  }

  /* Responsive */
  @media (max-width: 640px) {
    .about-hero h1 {
      font-size: var(--text-3xl);
    }

    .about-nav-grid {
      grid-template-columns: 1fr;
    }
  }
</style>